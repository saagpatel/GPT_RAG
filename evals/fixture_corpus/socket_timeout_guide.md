# Socket Timeout Guide

## Startup Failures

The most common local failure in this fixture corpus is a socket timeout during startup.
When that happens, inspect the local service boot logs and retry after the runtime is healthy.
